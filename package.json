{
  "name": "telegram-approval-cloud",
  "version": "1.0.0",
  "description": "Облачное решение для согласований через Telegram в Bitrix24",
  "main": "src/webapp/server.js",
  "scripts": {
    "start": "cd src/webapp && npm start",
    "dev": "cd src/webapp && npm run dev",
    "build": "cd src/webapp && npm run build",
    "install:webapp": "cd src/webapp && npm install",
    "setup": "node scripts/setup-cloud.js",
    "test": "cd src/webapp && npm test",
    "lint": "cd src/webapp && npm run lint"
  },
  "keywords": [
    "bitrix24",
    "telegram",
    "approval",
    "cloud",
    "rest-api",
    "webhook"
  ],
  "author": "Your Name",
  "license": "MIT",
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  }
}
